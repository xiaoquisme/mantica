name: mantica

services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: mantica
      POSTGRES_USER: ${POSTGRES_USER:-mantica}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mantica}
    ports:
      - "34532:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - ./data/minio:/data

volumes:
  pgdata:
